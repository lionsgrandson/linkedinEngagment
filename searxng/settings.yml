use_default_settings: true

general:
  debug: false
  instance_name: "Opportunity Hunter Search"

search:
  safe_search: 0
  autocomplete: ""
  formats:
    - html
    - json

server:
  secret_key: "opportunity-hunter-local-only"
  limiter: false
  public_instance: false
  image_proxy: false
